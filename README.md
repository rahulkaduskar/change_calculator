# Setup Instruction 

* Install Ruby version

* Install all gems
    ```
    bundle install
    ```

* Execute amount_change 
   ```
   ruby amount_change
   ```

*  To test ccode
   ```
   rspec
   ```
